/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js';
import * as beet from '@metaplex-foundation/beet';
import * as beetSolana from '@metaplex-foundation/beet-solana';

/**
 * Arguments used to create {@link LockupSettings}
 * @category Accounts
 * @category generated
 */
export type LockupSettingsArgs = {
  candyMachine: web3.PublicKey;
  lockupType: number;
  number: beet.bignum;
};

export const lockupSettingsDiscriminator = [245, 45, 181, 109, 28, 85, 201, 222];
/**
 * Holds the data for the {@link LockupSettings} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class LockupSettings implements LockupSettingsArgs {
  private constructor(
    readonly candyMachine: web3.PublicKey,
    readonly lockupType: number,
    readonly number: beet.bignum,
  ) {}

  /**
   * Creates a {@link LockupSettings} instance from the provided args.
   */
  static fromArgs(args: LockupSettingsArgs) {
    return new LockupSettings(args.candyMachine, args.lockupType, args.number);
  }

  /**
   * Deserializes the {@link LockupSettings} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0,
  ): [LockupSettings, number] {
    return LockupSettings.deserialize(accountInfo.data, offset);
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link LockupSettings} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
  ): Promise<LockupSettings> {
    const accountInfo = await connection.getAccountInfo(address);
    if (accountInfo == null) {
      throw new Error(`Unable to find LockupSettings account at ${address}`);
    }
    return LockupSettings.fromAccountInfo(accountInfo, 0)[0];
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey('cndy3Z4yapfJBmL3ShUp5exZKqR3z33thTzeNMm2gRZ'),
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, lockupSettingsBeet);
  }

  /**
   * Deserializes the {@link LockupSettings} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [LockupSettings, number] {
    return lockupSettingsBeet.deserialize(buf, offset);
  }

  /**
   * Serializes the {@link LockupSettings} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return lockupSettingsBeet.serialize({
      accountDiscriminator: lockupSettingsDiscriminator,
      ...this,
    });
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link LockupSettings}
   */
  static get byteSize() {
    return lockupSettingsBeet.byteSize;
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link LockupSettings} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment,
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(LockupSettings.byteSize, commitment);
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link LockupSettings} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === LockupSettings.byteSize;
  }

  /**
   * Returns a readable version of {@link LockupSettings} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      candyMachine: this.candyMachine.toBase58(),
      lockupType: this.lockupType,
      number: (() => {
        const x = <{ toNumber: () => number }>this.number;
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber();
          } catch (_) {
            return x;
          }
        }
        return x;
      })(),
    };
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const lockupSettingsBeet = new beet.BeetStruct<
  LockupSettings,
  LockupSettingsArgs & {
    accountDiscriminator: number[] /* size: 8 */;
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['candyMachine', beetSolana.publicKey],
    ['lockupType', beet.u8],
    ['number', beet.i64],
  ],
  LockupSettings.fromArgs,
  'LockupSettings',
);
